'use client';

import { ReactNode } from 'react';

export default function ClientLayout({ children }: { children: ReactNode }) {
  // You can use hooks and other client-side features here
  return <>{children}</>;
}
